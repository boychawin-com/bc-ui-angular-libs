
export const environment = {
  production: false,
  baseUrl: window.location.origin,
  baseApi: `/api/v1`,
  baseApiBatch: `/api/batchjob`,
  baseEmployeeUrl: `${window.location.origin}/api/ui/e`,
  basePartnerUrl: `${window.location.origin}/api/ui/e`,
  baseMyChannelUrl: `${window.location.origin}/api/ui/e`,
}

